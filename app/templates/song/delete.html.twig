{% include 'default/_form.html.twig' with {
    page_title: 'title.song_delete'|trans({'%id%': song.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'song_index',
} %}